# entity

JPA entities — `User`, `Movie`, `Genre`, `Industry`, `SubIndustry`, `UserMovieList`, `WatchPartyRoom`, `RoomParticipant`, `FriendRequest`, `RoomInvite`, `MovieRequest`, `Notification`.

See `BACKEND_DESIGN.md` at the repo root for field-level details per entity.
