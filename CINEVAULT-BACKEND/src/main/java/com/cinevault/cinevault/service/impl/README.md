# service/impl

Concrete implementations of the interfaces in `service` (e.g. `AuthServiceImpl`, `MovieServiceImpl`, `RoomServiceImpl`). This is where validation, business rules, and transaction boundaries actually live.
