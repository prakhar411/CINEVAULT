# controller

REST controllers, one per module (`AuthController`, `MovieController`, `AdminMovieController`, `RoomController`, `FriendController`, `RoomInviteController`, `MovieRequestController`, `AdminMovieRequestController`, `NotificationController`).
